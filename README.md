# Mini-Shell
Here's both the GitHub repo description and a full README:  GitHub repo description:      Unix shell implementation in C — handles I/O redirection, pipes, environment variables, and conditional execution. Built from scratch using fork, exec, dup2, and pipe syscalls.
